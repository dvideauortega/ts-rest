import * as chai from 'chai';

describe('Subtraction tests', () => { // the tests container
    it('2-2 = 0', () => {
        chai.expect(2-2).to.be.equal(0);
        chai.assert(2 + 2 == 4, "Two plus two should be equals to four")
    });

});